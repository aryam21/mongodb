users:
  - name: ksfeuser
    password: $MONGO_USER_PASSWORD

databases:
  - name: ksfetest
  - name: newdb

acl:
  - database: ksfetest
    user: ksfeuser
    access: dbOwner
  - database: newdb
    user: ksfeuser
    access: dbOwner
